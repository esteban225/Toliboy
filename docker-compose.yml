services:
  toliboy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toliboy-app
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - toliboy-network

networks:
  toliboy-network:
    driver: bridge
